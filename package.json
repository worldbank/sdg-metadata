{
  "name": "sdg-metadata",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "node scripts/prepare-build.js && cd www && bundle exec jekyll build",
    "serve": "node scripts/prepare-build.js && cd www && bundle exec jekyll serve"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gettext-parser": "4.0.2",
    "read-excel-file": "4.0.5",
    "xliff": "4.4.0",
    "xmlbuilder": "13.0.2",
    "yaml": "1.7.2"
  }
}
