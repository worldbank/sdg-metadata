{
  "name": "sdg-metadata",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "prepare": "node scripts/prepare-build.js && cp -r www/* _build && cp _build/api/all.json _build/_data",
    "build": "cd _build && bundle exec jekyll build",
    "serve": "cd _build && bundle exec jekyll serve"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gettext-parser": "4.0.2",
    "read-excel-file": "4.0.5",
    "xliff": "4.4.0",
    "xmlbuilder": "13.0.2",
    "yaml": "1.7.2"
  }
}
